# Import the data for the game and the artwork:
from art import logo, vs
from game_data import data
import random


items_in_data = len(data)
print(items_in_data)
#print(random.randint(range(1,items_in_data)))